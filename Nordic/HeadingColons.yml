extends: existence
message: "Capitalize '%s'."
link: https://docs.microsoft.com/en-us/style-guide/punctuation/colons
nonword: true
level: error
scope: heading
tokens:
  - ':\s(?!nRF)[a-z]'

## nRF is embedded in the regex to avoid false positives.
## It should be included in a separate exception rule list.
## Original regex:   - ':\s[a-z]'
